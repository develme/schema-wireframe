<a href="/{{&simple_name}}/create">New {{&proper_name}}</a>

<table>
    <thead>
            <tr>
                {{&data_heading}}
            </tr>
    </thead>
    <tbody>
        @foreach($_{{&simple_name}} as $_selected_{{&simple_name}})
            <tr>
                {{&data_body}}
            </tr>
        @endforeach
    <tbody>
</table>

